/**
 * AmarWave CDN entry point.
 * This file is bundled into amarwave.min.js / amarwave.umd.js.
 * It exposes only the AmarWave class as the default (and only) export,
 * so that `<script src="amarwave.min.js">` puts `window.AmarWave` directly.
 */
export { AmarWave as default } from './AmarWave';
